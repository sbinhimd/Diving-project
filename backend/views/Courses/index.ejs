<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <h1>Corses</h1>
        <ul>
            <% Corses.forEach( Corse => { %>
                <li>
                    The
                    <a href="/corses/<%=Corse.id; %>">
                        <%=Corse.CourseTitle; %>
                    </a>
                    <br/>
                   
                   <button> <a href="/corses/edit/<%=Corse.id%>">edit</a> </button> 
                    <form action="/corses/<%=Corse.id%>?_method=DELETE" method="POST">
                        <input type="submit" value="DELETE"/>
                    </form>
                </li>
            <% }) %>
        </ul>

        <nav>
            <a href="corses/new">Create a New Course</a>
        </nav>
    </body>
</html> 